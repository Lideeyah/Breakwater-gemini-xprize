# Local dev only. The dashboard runs on :3000 and the Breakwater proxy on :3001,
# so the browser's WebSocket must target the proxy's port explicitly.
# In production (single-origin behind one Cloud Run URL) this is unset and the
# dashboard connects same-origin.
NEXT_PUBLIC_PROXY_PORT=3001
